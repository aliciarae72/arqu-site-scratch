{
  "allow": {
    "filesize": [
      {
        "path": "home.html",
        "category": "declarative",
        "reason": "The page GitHub Pages serves: 357 lines of markup, 290 in one inline <style>, 875 in inline <script>. Extracting every inline block into the home-*.css and home-*.js siblings the repo already uses leaves about 360 lines of markup, and a static site with no build step cannot split markup further, so the 300-line cap is not reachable by refactoring. This entry is that issue's own option 2, recorded rather than skipped on every push. Tracked at https://github.com/aliciarae72/arqu-site-scratch/issues/3"
      }
    ],
    "tests": [
      {
        "path": "hail-grid.js",
        "category": "generated",
        "reason": "Written by scripts/build-hail-map.mjs from data/hail-severity.json — a run-length index with no logic in it. bin/test runs the generator's --check first, which regenerates in memory and fails when the committed file has drifted from the data, so the artifact is proved by the data rather than by a sibling test. test/home-hail-map.test.js resolves all 11,026 drawn cells through it."
      }
    ]
  },
  "commands": {
    "security": {
      "run": "pnpm audit --audit-level high"
    }
  }
}
